#include "main.h"
/**
  * _strlen - Returns the length of a string
  *
  * @s: String to count
  * 
  * Return: void
  */
void swap_int(char *s)
{
int i;

for (*s=0; *s !="\0"; s++){
i++;
}
return(i);
}
