#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* rev_string -  reverse a string
* @s: String to print
*
* Return: void
*/
void rev_string(char *s)
{
int i;
int leng;
leng = strlen(s);

for (i = leng - 1; i >= 0; i--)
{
printf("%c", s[i]);
}
printf("\n");
}
